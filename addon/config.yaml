name: Tri-Coach
description: Trainingsplanung für Ausdauersport (Laufen, Schwimmen, Radfahren, Triathlon)
version: 1.0.0
slug: tricoach
url: https://github.com/Molker86/tri-coach
arch:
  - aarch64
  - amd64
startup: application
boot: auto
ingress: true
ingress_port: 8000
panel_icon: mdi:run
panel_title: Tri-Coach
options:
  secret_key: ""
schema:
  secret_key:
    description: "Optional: JWT Secret Key für die Sessions. Wird automatisch generiert, wenn leer gelassen."
    example: "your-secret-key-here"
    type: string
