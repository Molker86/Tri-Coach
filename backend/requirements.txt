fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic[email]==2.10.4
pydantic-settings==2.7.0
PyJWT==2.10.1
python-multipart==0.0.20

# Garmin-Anbindung. `garminconnect` verlangt Python 3.12 — es bringt mit
# `curl_cffi` eine eigene TLS-Nachbildung mit, ohne die Garmins Cloudflare den
# Login abweist. `garth`, die frühere Grundlage, ist seit März 2026 tot.
garminconnect==0.3.10
# Verschlüsselt das Garmin-Token in der Datenbank (siehe app/crypto.py).
cryptography>=44
# Nur für das Zertifikatsbündel des Übungskatalog-Abrufs (garmin/katalog.py).
# Kommt ohnehin mit garminconnect mit — steht hier, weil wir es jetzt direkt
# importieren und eine transitive Abhängigkeit dafür kein Versprechen ist.
certifi

# Bring-Einkaufsliste. Dieselbe Bibliothek, die Home Assistant benutzt — die
# Bring-API ist undokumentiert, und den Anmeldeablauf samt API-Schlüsseln und
# Token-Erneuerung selbst nachzubauen wäre Pflegeaufwand ohne Gegenwert.
# Sie ist async (aiohttp); wie das zum synchronen Rest passt, steht in
# app/bring/client.py.
bring-api==1.1.2
aiohttp>=3.13
