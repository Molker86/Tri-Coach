# Nur zum Entwickeln und Testen — bewusst getrennt von requirements.txt, damit
# das Docker-Abbild (und damit der Build auf dem Raspberry Pi) schlank bleibt.
#
# Einrichten:  backend/.venv/bin/pip install -r backend/requirements-dev.txt
-r requirements.txt

pytest==8.3.4
# `fastapi.testclient` baut auf httpx auf. Ohne das Paket bricht schon das
# Einsammeln der Tests ab — die App selbst braucht es nicht.
httpx==0.28.1
